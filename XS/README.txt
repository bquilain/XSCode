CompareCalibrationsPM: From MC and Data samples, generate a file format which can be easily compared using CompareDataMC_Draw.c

GeneratePDFMuCL: Generate the PDF distribution for the MuCL: ./GeneratePDFMuCL -i intial_file_number (e.g, 1) -f final_file_number (e.g, 100) -m

XS_CC0pi: Change the format from INGRID data to XS format.

